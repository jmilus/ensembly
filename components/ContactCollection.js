'use client'

const ContactCollection = ({groups}) => {
    console.log({ groups });

    return (
        <div className="input-control-base"> Addressees</div>
    )
}

export default ContactCollection;